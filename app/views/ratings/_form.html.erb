
<p class="error"></p>
<%= form_with model: @rating do |f| %>
	<%= f.label "Rating (out of 5)" %>
	<%= f.number_field :star, min: 1, max: 5 %> <br>
	<%= f.label :review %>
	<%= f.text_area :review %>
	<%= f.hidden_field :movie_id, value: @movie.id %> <br>
	<%= f.submit %>
<% end %>